# Копия живёт ТОЛЬКО на сервере как /opt/boss-brand/.env — никогда не коммитить
# реальные значения в этот репозиторий (он публичный).
TELEGRAM_BOT_TOKEN=123456:AA...
SESSION_SECRET=сгенерировать: openssl rand -hex 32
